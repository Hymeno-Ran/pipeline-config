properties(
  [
    parameters([
      # string(name: 'a', defaultValue: 'a', description: 'Parameter a'),
      # string(name: 'b', defaultValue: 'b', description: 'Parameter b'),
      # string(name: 'c', defaultValue: 'c', description: 'Parameter c'),
      # string(name: 'd', defaultValue: 'd', description: 'Parameter d'),
      string(name: 'repo', defaultValue: 'd', description: 'Parameter d')
    ])
  ]
)

node {
  build(a='e',b='f',c='g',d='h')
  echo "start"
}
